# 尼沙皇版图浏览小工具 v1.0.1

三个新功能 + 四处修复。

## 这个版本加了什么

- **箭头中段输入字符（可选）**：画完箭头直接在箭头中点弹输入框，`Esc` 跳过就是普通箭头；写上的字符锁在中点、始终水平、跟着箭头一起拖动，箭头和字符算一步撤销。切到选择工具双击箭头还能补字或改字。
- **地图标记文字**：新增 `T` 工具，点地图就地输入（回车落字、`Esc` 取消、中文输入法不受影响），字号 16 / 24 / 36 三档、颜色沿用 6 色调色板，文字带深色描边。选中后能拖动、改颜色、改字号、删除，双击可改内容。
- **橡皮擦**：新增 `E` 工具，按住左键划过要删的标注就擦掉，擦掉的整段拖动算一步撤销。
- **赛季文件刷点**：地图左下先加了入口占位，功能还在做（点一下提示在施工）。
- **更新公告**：启动时弹一次本版本公告，可以勾「不再提醒」；公告内容放在 `config/announcements.json`，以后改公告不用改代码。右下角版本号旁边新增「公告」按钮，勾过不再提醒也能随时再翻。

## 这个版本修了什么

- **点地图输不了字**：鼠标点下去时浏览器会把焦点抢回页面，输入框刚弹出就被当成失焦关掉了。现在弹框后会重新把光标抢回输入框，并且只在真正点到别处时才确认。
- **改不了已有的字**：文字工具开着的时候点已有的文字会另开一个空输入框（等于又新建一条），现在改成「点已有的字就是改它」，文字工具常开也能改。
- **启动偶发卡住**：等首屏两帧的动画回调在窗口被遮挡/重载时可能一直不触发，把启动流程挂死；现在加了超时兜底，另外给地图图片加载也加了超时。
- **撤销会丢任务**：用画笔/鼠标画完一笔后按 `Ctrl+Z`，旧的历史快照里漏了任务表，会把所有任务框一起清掉；现在撤销只回退这一笔，任务不动。

## 下载

| 文件 | 大小 | SHA256 |
| --- | --- | --- |
| `尼沙皇版图浏览小工具-1.0.1-portable.exe` | 122.8 MB | `DA627583823415F95B8D073775FF1D219FD91B7AD059A624DFFB1BA4C62BC820` |

## 版本档案

| 版本 | 归档目录 | 免安装 exe SHA256 |
| --- | --- | --- |
| v1.0.0 | `archive/v1.0.0/` | `2271C03AD01F6681A731C0E9BF3147469A1F1E8CFB9E9A7DD3B3EB7924AEE6F5` |
| v1.0.1 | `archive/v1.0.1/` | `DA627583823415F95B8D073775FF1D219FD91B7AD059A624DFFB1BA4C62BC820` |

归档与回滚做法见 `archive/README.md`：把 `archive/v<版本>/source/` 覆盖回工程即可回到那一版。

---

# 尼沙皇版图浏览小工具 v1.0.0

第一个公开版本。一个给《逃离塔科夫》玩家用的**离线地图浏览小工具**：诺文斯克大地图上点地区进详情，随手画圈画框记任务，所有数据只存在自己电脑上。

## 下载

| 文件 | 大小 | SHA256 |
| --- | --- | --- |
| `尼沙皇版图浏览小工具-1.0.0-portable.exe` | 122.7 MB | `2271C03AD01F6681A731C0E9BF3147469A1F1E8CFB9E9A7DD3B3EB7924AEE6F5` |

双击即可运行：免安装、不写注册表、不需要装运行库，删掉 exe 就是卸载。系统要求 Windows 10 / 11（64 位）。

> 单文件版每次启动会先自解压到临时目录（约 5～7 秒，期间显示一张启动图），退出后自动清理。想秒开的话，用 7-Zip 把 exe 解开，直接跑里面的 `尼沙皇版图浏览小工具.exe`。

## 这个版本有什么

- **诺文斯克大地图**：13 个地区热区，鼠标移上去高亮、点进去看详情；热区位置可拖动校准并自动保存。
- **地区地图**：滚轮以光标为中心缩放、拖拽平移、双击或 `Ctrl+0` 适合窗口、`100%` 看原始像素。
- **标注**：圆 / 方框 / 箭头 / 自由画笔，6 色 3 档线宽，撤销重做，单条选择后可拖动改色删除，整层可隐藏。
- **跨地图任务框**：一个任务可以在多张地图上框地点，做完点一下打勾，大地图上会用任务色标出哪些地区挂着它。
- **界面**：右上角 `☰`（或 `H`）收起整块右侧 UI；「📋 复制视图」把当前画面隐去界面元素后复制进剪贴板。
- **彩蛋**：`🎁 scav宝箱` 抽一把 scav 带回来的物资（价格取自 eftarkov.com 快照，纯娱乐）、`🕺 尼沙皇跳舞`。
- **数据全在本机**：标注 / 校准 / 偏好分别存成 `%APPDATA%\尼沙皇版图浏览小工具\` 下的 `annotations.json`、`hotspots.json`、`settings.json`，程序全程不联网。

## 注意事项

- `%APPDATA%` 目录名跟着应用名走：从开发版「尼沙皇吊图地图浏览」升级上来时，第一次启动会自动把旧的三个 json 搬进新目录，原目录不会删。
- 地图是静态图片（游戏画面 + 社区资料），不是实时地图；个别地区标了「暂不可用」。
- scav 宝箱的出场概率是按价格调过的娱乐数值，不代表游戏里的真实掉落率。

## 从源码构建

```powershell
npm install
npm start            # 开发模式
npm run smoke        # 端到端自检（100 项）
npm run dist         # 打包单文件到 dist/
```

## 作者

一只非常屑的彩虹滑稽 · [B 站主页](https://space.bilibili.com/500638825?spm_id_from=333.1007.0.0)

代码以 MIT 许可开源，欢迎自取改造。

「Escape from Tarkov」及相关素材、商标归 Battlestate Games 所有；本项目为非商业的个人作品，仅供交流学习。
